import React from 'react';

const NotFound = () => {
    return(
        <div>
            <h1 className="main-title">We are sorry, we couldn't find the page you are looking for</h1>
        </div>
    );
};

export default NotFound;